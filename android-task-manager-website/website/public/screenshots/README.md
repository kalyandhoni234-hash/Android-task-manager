Drop real Android Task Manager screenshots here as PNGs, then pass a
`src="/screenshots/<file>.png"` prop into the corresponding
`ScreenshotFrame` usage in `components/Hero.tsx`,
`components/DashboardShowcase.tsx`, and `components/ProcessInspector.tsx`.

Suggested filenames referenced in the copy:
- dashboard.png
- cpu-panel.png
- process-inspector.png

Until real screenshots are added, each of those sections renders an
honest "screenshot pending" placeholder instead of a fabricated mockup.
